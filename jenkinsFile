pipeline{
    agent any 
    stages{
        stage('Run Playbook'){
            steps{
                
                sh "ansible-playbook -i configuration/inventory.yaml configuration/playbook.yaml --ask-become-pass"
            }

        }
    }
}