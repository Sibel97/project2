---
# tasks file for initialInstalls
- name: "update apt packages"
  apt:
    upgrade: yes
    update_cache: 'True'

- name: "install git and python3 and python pip"
  become: 'True'
  apt:
    name:
    - python3
    - python3-pip
    - git 
    state: latest
    update_cache: 'True'

